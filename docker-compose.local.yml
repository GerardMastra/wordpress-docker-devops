services:
  php:
    volumes:
      - ${RUNTIME_DIR}/wordpress:/var/www/html # Solo activo en LOCAL
  nginx:
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.http.conf:/etc/nginx/conf.d/default.conf
      - ${RUNTIME_DIR}/wordpress:/var/www/html:ro
